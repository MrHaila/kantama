import { describe, it } from 'vitest';

describe('export', () => {
  it.todo('should export routes to JSON');
  it.todo('should create nested format (from → to → duration)');
  it.todo('should filter by period');
});
